<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bouchef.tubolsillo">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="TU BOLSILLO"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" android:configChanges="orientation|screenSize"/>

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
         The API key for Google Maps-based APIs is defined as a string resource.
         (See the file "res/values/google_maps_api.xml").
         Note that the API key is linked to the encryption key used to sign the APK.
         You need a different API key for each encryption key, including the release key that is used to
         sign the APK for publishing.
         You can define the keys for the debug and release targets in src/debug/ and src/release/.
    -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <!--<activity android:name=".HomeActivity" />-->
        <activity android:name=".AquiEstoyPCD" android:configChanges="orientation|screenSize"/>
        <activity android:name=".ArmarListaTutor" android:configChanges="orientation|screenSize"/>
        <activity android:name=".AutorizarTutor" android:configChanges="orientation|screenSize"/>
        <activity android:name=".PagarPCD" android:configChanges="orientation|screenSize"/>
        <activity android:name=".CredencialPCD" android:configChanges="orientation|screenSize"/>
        <activity android:name=".DondeEstasTutor" android:configChanges="orientation|screenSize"/>
        <activity android:name=".EvaluadorTutor" android:configChanges="orientation|screenSize"/>
        <activity android:name=".HistorialTutor" android:configChanges="orientation|screenSize" />
        <activity android:name=".ListaCompraPCD" android:configChanges="orientation|screenSize"/>
        <activity android:name=".LocationActivity" android:configChanges="orientation|screenSize"/>
        <activity android:name=".MiListaTutor" android:configChanges="orientation|screenSize"/>
        <activity android:name=".InicioPCD" android:configChanges="orientation|screenSize"/>
        <activity android:name=".InicioTutor" android:configChanges="orientation|screenSize"/>
        <activity android:name=".NotificadorPCD" android:configChanges="orientation|screenSize"/>
        <activity android:name=".SeguimientoCompra" android:configChanges="orientation|screenSize"/>
        <activity android:name=".ListaNegociosFavoritos" android:configChanges="orientation|screenSize"/>
        <activity android:name=".ListaControlGastos" android:configChanges="orientation|screenSize"/>
        <activity android:name=".BotoneraInicialPCD" android:configChanges="orientation|screenSize"/>
        <activity android:name=".BotoneraInicialAyudante" android:configChanges="orientation|screenSize"/>
        <activity android:name=".HomeActivityTutor" android:configChanges="orientation|screenSize"/>
        <activity android:name=".MainActivity2" android:configChanges="orientation|screenSize"/>

    </application>

</manifest>